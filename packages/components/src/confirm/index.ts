/** @format */

import _Confirm from './confirm.vue';
import { Confirm } from './confirm';
import { default as withInstall } from '../../utils/withinstall';

export const EcConfirm = withInstall(_Confirm, Confirm);
export default EcConfirm;
